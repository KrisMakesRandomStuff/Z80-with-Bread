#pragma once
#include <Arduino.h>

#define MAX_COMMAND_LENGTH 10

uint8_t parse_command(char* buff, char* command, uint8_t command_len) {
    for(int i = 0; i < command_len; i++) {

    }

    return 0;
}